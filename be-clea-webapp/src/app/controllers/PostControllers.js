const Post = require("../models/Post");

class PostControllers {
  //[get]/api/posts/:id
  index(req, res) {}
  //[get]/api/posts
  //[post]/api/posts
  //[put]/api/posts/:id
  //[delete]/api/posts/:id
}

module.exports = new PostControllers();
